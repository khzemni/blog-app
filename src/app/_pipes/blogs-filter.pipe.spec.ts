import { BlogsFilterPipe } from './blogs-filter.pipe';

describe('BlogsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BlogsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
